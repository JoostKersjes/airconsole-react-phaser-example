// Get AirConsole API from globals
export const AirConsole = window.AirConsole;

// Export the API instance
export const airconsole = new AirConsole({
  // Configure the API
  orientation: AirConsole.ORIENTATION_LANDSCAPE
});
